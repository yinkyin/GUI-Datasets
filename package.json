{
  "name": "gui-datasets-repo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "setup": "npm --prefix site install",
    "dev": "npm --prefix site run dev --",
    "build": "npm --prefix site run build",
    "preview": "npm --prefix site run preview --",
    "classify": "node scripts/classify-dataset.mjs"
  }
}
